# Gateway Loadtest Baseline (2026-04-22)

## 1. 执行信息

- 执行时间（UTC）：`2026-04-22T14:16:09.865411850Z`
- 执行入口：`tools/loadtest-alert-closure.sh`
- 报告原始文件：`build/reports/loadtest/gateway-pipeline-loadtest.json`
- 摘要文件：`build/reports/loadtest/gateway-pipeline-loadtest-summary.md`

## 2. 压测场景

- 会话数：`24`
- 每会话帧数：`180`
- 总帧数：`4320`
- 模拟链路：`session.start -> audio.frame -> asr.partial -> translation.result -> session.control(CLOSED)`
- 验收门槛：`successRatio >= 0.999`
- 验收门槛：`frameLatencyMsP95 <= 1500`

## 3. 基线结果

| 指标 | 值 |
| --- | --- |
| successfulFrames | `4320` |
| failedFrames | `0` |
| successRatio | `1.000000` |
| elapsedMs | `576` |
| throughputFramesPerSecond | `7492.7` |
| frameLatencyMsP50 | `0` |
| frameLatencyMsP95 | `0` |
| frameLatencyMsP99 | `0` |
| frameLatencyMsMax | `28` |
| subtitlePartialCount | `4320` |
| subtitleFinalCount | `4320` |
| sessionClosedCount | `24` |
| gatewayWsErrorCount | `0` |
| downlinkErrorCount | `0` |

## 4. 风险解释

- 当前基线来自仓库内 in-memory harness，不包含真实网络、Kafka broker 抖动、外部 ASR/翻译/TTS 引擎时延。
- `P95=0ms` 仅说明该测试夹具下无明显处理阻塞，不能直接当生产延迟承诺。
- 这次阈值收紧用于把“明显退化”尽早暴露；生产阈值必须基于预发/线上真实数据再标定。

## 5. 后续动作

1. 每次涉及网关吞吐、下行映射、流控参数变更时，重新执行 `tools/loadtest-alert-closure.sh` 并对比本报告。
2. 进入真实引擎联调阶段后，追加端到端压测报告（含 Kafka 与外部引擎）。
3. 告警值班执行路径按 `docs/runbooks/loadtest-alert-closure.md`。
